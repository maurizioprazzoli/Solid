using System;

namespace OpenClose
{
    class PerimeterPrinter
    {
        public void PrintTotalPerimeter(double totalPerimeter)
        {
            Console.WriteLine($"Il perimetro totale degli oggetti è: {totalPerimeter}");
        }
    }
}
